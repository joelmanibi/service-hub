const Joi = require('joi');

/**
 * Schémas de validation du module Dashboard (tableau de bord).
 * Responsabilité : valider les payloads (body/query/params) entrants
 * du module avant qu'ils n'atteignent le contrôleur, via le middleware
 * middlewares/validate.js. Aucun schéma métier défini à ce stade
 * (architecture uniquement).
 */

module.exports = {
  // TODO: définir les schémas (ex: createSchema, updateSchema...)
};
