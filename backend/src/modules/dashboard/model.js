/**
 * Modèle(s) Sequelize du module Dashboard (tableau de bord).
 * Responsabilité : définir le schéma de données et les associations de
 * l'entité (ou des entités) propre(s) à ce module. Chargé dynamiquement
 * par database/index.js. Aucun champ métier défini à ce stade
 * (architecture uniquement).
 */

module.exports = (sequelize, DataTypes) => {
  // TODO: return sequelize.define('NomEntite', { /* champs */ });
  return null;
};
