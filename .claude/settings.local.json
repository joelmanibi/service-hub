{
  "permissions": {
    "allow": [
      "PowerShell(Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue | Where-Object { $_.LocalPort -eq 3005 } | Select-Object LocalPort,OwningProcess)"
    ]
  }
}
